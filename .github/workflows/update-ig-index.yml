name: Update IG index

on:
  repository_dispatch:
    types: [ig-deployed]

permissions:
  contents: write

jobs:
  update-index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Update index.html with IG card
        env:
          IG_NAME: ${{ github.event.client_payload.ig_name }}
          IG_VERSION: ${{ github.event.client_payload.ig_version }}
        run: |
          python3 - <<'EOF'
          import os, re, json

          ig_name = os.environ["IG_NAME"]
          ig_version = os.environ.get("IG_VERSION", "")

          base = f"ig/{ig_name}/{ig_version}" if ig_version else f"ig/{ig_name}"

          with open(f"{base}/package.manifest.json", encoding="utf-8") as f:
              manifest = json.load(f)

          package_id = manifest["name"]
          ig_version = ig_version or manifest.get("version", "")

          with open(f"{base}/ImplementationGuide-{package_id}.json", encoding="utf-8") as f:
              ig_res = json.load(f)

          ig_title = ig_res.get("title", ig_name)
          ig_description = ig_res.get("description", "")

          card_id = f"ig-card-{ig_name}"
          href = f"/ig/{ig_name}/{ig_version}/" if ig_version else f"/ig/{ig_name}/"
          tag_html = f'<span class="tag">v{ig_version}</span>' if ig_version else ""

          new_card = (
              f'    <a class="ig-card" id="{card_id}" href="{href}">\n'
              f'      <h3>{ig_title}</h3>\n'
              f'      <p>{ig_description}</p>\n'
              f'      {tag_html}\n'
              f'    </a>'
          )

          with open("index.html", "r", encoding="utf-8") as f:
              html = f.read()

          # Remove old card for this IG if it exists
          pattern = rf'    <a class="ig-card" id="{re.escape(card_id)}"[\s\S]*?</a>'
          html = re.sub(pattern, "", html)

          # Remove empty-state placeholder if present
          empty_pattern = r'\s*<div class="empty-state">[\s\S]*?</div>'
          html = re.sub(empty_pattern, "", html)

          # Insert new card before closing of ig-grid div
          html = html.replace(
              "  </div>\n</main>",
              f"\n{new_card}\n  </div>\n</main>"
          )

          with open("index.html", "w", encoding="utf-8") as f:
              f.write(html)

          print(f"Added/updated card for {ig_name} ({ig_title} v{ig_version})")
          EOF

      - name: Commit and push
        env:
          IG_NAME: ${{ github.event.client_payload.ig_name }}
          IG_VERSION: ${{ github.event.client_payload.ig_version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "chore: update index for ${IG_NAME} ${IG_VERSION}"
          git push origin gh-pages
